# HTTP-only reverse proxy. Source of truth for the HTTP server is http.conf
# (copied into the image). When Let's Encrypt certs exist, docker-entrypoint.sh
# switches to https.conf instead. Do not list a real domain here.
#
# See docker/nginx/http.conf, docker/nginx/https.conf and docs/staging-domain-https.md.
